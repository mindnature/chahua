# AGENTS.md

本仓库的主工作流是自然材料拼贴内容生产。

当用户提出以下任一意图时，必须先读取并遵循 `skills/chahua-pipeline/SKILL.md`：

- 运行 / 执行 chahua、插画、手工画、自然材料拼贴流程；
- 每日选一张风景或街景照片制作拼贴；
- 在 ChatGPT 中把照片转换成自然材料拼贴海报；
- “去掉上半部分”并保存最终成品；
- 为成品生成小红书五字内标题与简介；
- 在 Gemini 中把拼贴成品制作成真实手工过程视频；
- 从上次中断处继续、断点续跑或批量处理。

默认原则：

1. 默认一次只处理 1 张，使用 `FAST` 成功路径。
2. 不覆盖原图。
3. 优先恢复已完成阶段，禁止无意义重复生成。
4. 选图只依赖公开网络搜索；浏览器 UI 自动化优先按可见语义定位，不依赖固定坐标。
5. 正常路径不频繁截图、不做全 DOM 扫描；只有失败才进入诊断模式。
6. Gemini 视频生成的唯一标准入口是：`打开/新建对话 → 打开左侧边栏 → 点击“视频” → 确认进入视频生成界面`。进入视频模式后，才允许选择 3.8 Flash + Extended Thinking、设置 9:16、上传图片、输入提示词。不得用输入框 `+`、上传和工具菜单、普通聊天或 `/videos` 视频库替代该入口。
7. 视频提示词发送前必须确认输入框不是空的，并验证完整提示词已实际注入。
8. 视频画幅硬约束为 9:16。
9. 指定模型 / 选项不存在时停止并报告，不静默替换。
10. Gemini 视频成功生成并下载到本地后即结束 VIDEO 阶段。默认不抽帧、不播放、不做内容 QA、不等待额外版本。
11. 只有用户明确要求“视频 QA / 抽帧检查”时才执行视频内容检查。

固定提示词不得从 AGENTS.md 复制或改写；始终读取：

- `skills/chahua-pipeline/prompts/poster.md`
- `skills/chahua-pipeline/prompts/video.md`
